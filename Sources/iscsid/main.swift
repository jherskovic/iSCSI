import Foundation
import iSCSIKit

// iscsid — iSCSI initiator daemon. Placeholder until Phase 4:
// launchd integration, XPC control surface, session management, backends.
FileHandle.standardError.write(Data("iscsid: not yet implemented (Phase 4)\n".utf8))
exit(1)
